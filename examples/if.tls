(true => bool True)
(false => bool False)


(if (:literal true) a b => a)
(if (:literal false) a b => b)

(assert a b => print (if (eq a b) (success a is b) (error a is-not-equal-to b)))

(assert someValue someOtherValue)
(assert foo foo)

(print (if (eq (a(b)) (a(b))) booleans-work-again booleans-don't-work))
(print (if (eq (a(c)) ((a(v)))) booleans-don't-work booleans-work))

(printBool (bool a) => print bool a)
(printBool true)

if (eq 1 1) (print correct) (print non-correct)
